name: linter
description: "A shared package for managing linting rules across the workspace."
version: 1.0.1

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  very_good_analysis: ^10.2.0