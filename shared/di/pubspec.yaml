name: di
description: "Reusable DI framework — IsolatedScope base class, ScopeProvider InheritedWidget, and ScopeWidget lifecycle manager."
version: 1.1.2
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  get_it: ^9.2.1
  injectable: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  linter:
    git:
      url: https://github.com/arkariz/advance-mobile-platform
      ref: linter-v1.0.2
