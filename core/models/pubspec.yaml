name: models
description: "A Flutter package for core models."
version: 1.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  dependencies:
    git:
      url: https://github.com/arkariz/advance-mobile-platform
      path: shared/dependencies
      ref: dependencies-v1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  linter:
    git:
      url: https://github.com/arkariz/advance-mobile-platform
      ref: linter-v1.0.1