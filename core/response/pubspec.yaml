name: response
description: "A shared package for handling API responses, including error handling and pagination, to ensure consistent response structures across the application."
version: 1.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

dependencies:
  dependencies:
    git:
      url: https://github.com/arkariz/advance-mobile-platform
      ref: dependencies-v1.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: any
  flutter_test:
    sdk: flutter
  json_serializable: any
  linter:
    git:
      url: https://github.com/arkariz/advance-mobile-platform
      ref: linter-v1.0.1