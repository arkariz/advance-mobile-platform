name: dio_network
description: "Network module for handling API calls and network-related operations."
version: 1.3.2
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.4
  flutter: ">=1.17.0"

dependencies:
  api_network:
    git:
      url: https://github.com/arkariz/advance-mobile-platform
      path: infrastructure/network/api_network
      ref: api_network-v1.2.2
  dependencies:
    git:
      url: https://github.com/arkariz/advance-mobile-platform
      path: shared/dependencies
      ref: dependencies-v1.3.0
  failures:
    git:
      url: https://github.com/arkariz/advance-mobile-platform
      path: core/failures
      ref: failures-v2.0.1
  flutter:
    sdk: flutter
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  linter:
    git:
      url: https://github.com/arkariz/advance-mobile-platform
      ref: linter-v1.0.2